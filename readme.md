Name:
Aaron Fore

I used ChatGPT for help when writing the standard redirections' code, a portion of the tokenizer, and errno - as well as general syntax throughout

Description:
Shell written in C

Instructions to clone, compile, and run:

Written on UNIX
Clone (git) - in terminal : git clone https://github.com/amf080/techshell 
Compile (gcc) - in techshell folder in terminal : gcc techshell.c -o techshell
Run in terminal : ./techshell

Has cd, exit, system calls, and basic input/output redirection
